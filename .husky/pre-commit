#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

dotnet husky run --group pre-commit

# dotnet-format above edits staged files in place; re-stage what was
# already staged so the fixes land in this commit instead of blocking it.
git update-index --again
